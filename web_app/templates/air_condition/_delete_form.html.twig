<form method="post" action="{{ path('app_air_condition_delete', {'id': air_condition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ air_condition.id) }}">
    <button class="btn">Delete</button>
</form>
